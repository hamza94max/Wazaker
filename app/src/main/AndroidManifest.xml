<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hazem.wazkar">

    <application
        android:name="com.hazem.wazaker.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name="com.hazem.wazaker.recevier.NotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <activity android:name="com.hazem.wazaker.activities.FortyActivity" />

        <activity android:name="com.hazem.wazaker.activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.hazem.wazaker.activities.MainActivity"
            android:launchMode="singleTop" />
        <activity android:name="com.hazem.wazaker.activities.AzkarActivity" />
    </application>

</manifest>